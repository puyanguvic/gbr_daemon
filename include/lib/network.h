// #ifndef NETWORK_H
// #define NETWORK_H

// #include <netinet/in.h>

// // 创建用于 OSPF 通信的原始套接字
// int create_ospf_socket();

// // 绑定套接字到指定地址
// int bind_socket(int sockfd, struct sockaddr_in *addr);

// // 加入多播组，用于接收 OSPF 多播消息
// int join_multicast_group(int sockfd, const char *multicast_ip, const char *interface);

// #endif // NETWORK_H
